name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  hello-world:
    runs-on: self-hosted
    steps:
      - run: echo "hello world"
      - run: echo "goodbye"
